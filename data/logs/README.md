# Historical runtime logs

These logs document earlier Ring01 runs. They are grouped here to keep the repository root and phase directories focused on source code and accepted artifacts.

The historical logs directly in this directory are intentionally retained. Put temporary output under `data/logs/local/`, or use a log path elsewhere; newly generated logs are ignored by Git. Important outcomes should be captured in a phase JSON report or experiment log instead of relying only on console output.
